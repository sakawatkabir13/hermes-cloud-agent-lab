# Copy to: ~/.hermes/.env
# Never commit the real .env file.

TELEGRAM_BOT_TOKEN=replace_with_botfather_token
TELEGRAM_ALLOWED_USERS=replace_with_numeric_telegram_user_id

# Optional provider tokens go here, depending on your Hermes provider setup.
# GITHUB_TOKEN=replace_with_token
# COPILOT_TOKEN=replace_with_token
# OPENAI_API_KEY=replace_with_key
# ANTHROPIC_API_KEY=replace_with_key
